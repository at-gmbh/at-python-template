# This file defines the requirements for a productive project deployment.
# It should not contain dependencies for tests, developer tools, etc.
# For development-related dependencies, please refer to requirements-dev.txt.

# Dependency for configuration file format
{% if cookiecutter.config_file == 'hocon' %}
# HOCON configuration parser
pyhocon~=0.3.61
{% elif cookiecutter.config_file == 'yaml' %}
# YAML configuration parser
PyYAML~=6.0.2
{% endif %}

# Dependency for Command-Line Interface (CLI) if enabled
{% if cookiecutter.create_cli == 'yes' %}
# CLI framework (with optional dependencies for enhanced functionality)
typer~=0.16.1
{% endif %}

# AI Starter Kit Dependencies
{% if cookiecutter.include_ai_starter_kit == 'yes' %}
# LangChain core and providers for AI/LLM workflows
langchain>=0.3
langchain-openai>=0.2
langchain-community>=0.3
# Data validation and structured outputs
pydantic>=2.10
pydantic-settings>=2.7
# Environment variable management
python-dotenv>=1.0
# Enhanced logging
loguru>=0.7
# Vector database for RAG
chromadb>=0.6
# OpenAI tokenizer
tiktoken>=0.8
{% endif %}

# UI Framework Dependencies
{% if cookiecutter.ui_framework == 'chainlit' %}
# Chainlit for async chatbot interfaces
chainlit>=1.3
{% elif cookiecutter.ui_framework == 'streamlit' %}
# Streamlit for data app interfaces
streamlit>=1.40
{% endif %}
